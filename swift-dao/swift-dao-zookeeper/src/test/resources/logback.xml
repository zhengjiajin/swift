<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="5 seconds">

	<jmxConfigurator />
	
	<property name="pattern" value="%date %5level %thread %class{0}:%line - %message%n" />
	<property name="fileNamePattern" value="%d{yyyyMMddHH}.%i" />
	<property name="maxHistory" value="365" />
	<property name="maxFileSize" value="20MB" />

	<appender name="stdout" class="ch.qos.logback.classic.AsyncAppender">
		<appender name="stdout0" class="ch.qos.logback.core.ConsoleAppender">
			<Target>System.out</Target>
			<encoder><pattern>${pattern}</pattern></encoder>
		</appender>
		<queueSize>10000</queueSize>
		<discardingThreshold>1</discardingThreshold>
		<includeCallerData>true</includeCallerData>
	</appender>

	<appender name="file" class="ch.qos.logback.classic.AsyncAppender">
		<appender name="file0" class="ch.qos.logback.core.rolling.RollingFileAppender">
			<File>log/log_archive_.log</File>
			<encoder><pattern>${pattern}</pattern></encoder>
			<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
				<FileNamePattern>log/log_archive_.log.${fileNamePattern}</FileNamePattern>
				<MaxHistory>${maxHistory}</MaxHistory>
				<TimeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
					<MaxFileSize>${maxFileSize}</MaxFileSize>
				</TimeBasedFileNamingAndTriggeringPolicy>
			</rollingPolicy>
		</appender>
		<keepingLevel>OFF</keepingLevel>
		<queueSize>10000</queueSize>
		<discardingThreshold>1</discardingThreshold>
		<includeCallerData>true</includeCallerData>
	</appender>

	<root level="INFO">
		<!--<appender-ref ref="file" />-->
		<appender-ref ref="stdout" />
	</root>
</configuration>
